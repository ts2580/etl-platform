dependencies {
    // Shared util/helpers across modules

    // Used by shared Salesforce OAuth client
    api libs.okhttp
    api libs.jacksonDatabind
    api 'jakarta.validation:jakarta.validation-api'

    compileOnly libs.lombok
    annotationProcessor libs.lombok
}
